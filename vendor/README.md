# vendor/wps365-read

本目录内置 **wps365-read**（WPS 云文档 Drive CLI），供周报迁移 Skill 读写 `.ksheet` 使用。

- 用户 `git clone` 本仓库后**无需**再单独安装或搜索 wps365-read
- 预检会优先使用 `vendor/wps365-read/skills/drive/run.py`
- 凭证使用本 Skill 根目录的 `assets/config/auth.yaml`（勿在此目录放 `auth.yaml`）
